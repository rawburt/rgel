(library
 (name rgel)
 (preprocess
  (pps ppx_deriving.show))
 (modules
  debug
  location
  parsed_ast
  typed_ast
  grammar
  lexer
  errors
  type_check
  types
  codegen))

(menhir
 (modules grammar))

(ocamllex lexer)
